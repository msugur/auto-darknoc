apiVersion: v1
kind: Secret
metadata:
  name: notification-credentials
  namespace: dark-noc-gitops
type: Opaque
stringData:
  NOTIFY_EMAIL: "msugur@redhat.com"
  SMTP_HOST: "REPLACE_WITH_SMTP_HOST"
  SMTP_PORT: "587"
  SMTP_USER: "REPLACE_WITH_SMTP_USER"
  SMTP_PASSWORD: "REPLACE_WITH_SMTP_PASSWORD"
