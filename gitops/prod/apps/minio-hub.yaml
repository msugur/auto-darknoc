apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: minio-hub
  namespace: openshift-gitops
  annotations:
    argocd.argoproj.io/sync-wave: "-4"
spec:
  project: auto-darknoc-prod
  source:
    repoURL: https://github.com/msugur/auto-darknoc.git
    targetRevision: main
    path: gitops/prod/stacks/hub/minio
  destination:
    name: in-cluster
    namespace: dark-noc-minio
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - SkipDryRunOnMissingResource=true
